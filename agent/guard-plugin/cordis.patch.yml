- insert:
    - id: agent3-guard
      name: '@hunter-zk/agent3-guard'
      # Audit delivery is intentionally not configured here. The previous
      # /api/v1/agent/audit target did not exist and silently dropped every
      # best-effort fetch. A production audit sink will be designed with
      # authentication, persistence and trace identity instead of pointing the
      # Guard at a fictitious Portal route.
      config: {}
