# 11 · P0 acceptance

1. `python scripts/verify.py` succeeds.
2. Backend starts and `/health` returns `status=ok`.
3. `/api/v1/tables` returns rich synthetic assets with stable asset IDs.
4. `/api/v1/search?q=贷款余额` and `q=dwd_loan` return relevant assets.
5. Browser root displays the design-system prototype.
6. Dataset detail includes scheduling as detail metadata; there is no standalone scheduling calendar.
7. Repository contains no Java/Spring/MyBatis/Maven implementation baseline.
8. External Agent chain is not falsely marked passed without a configured dsh/Agent3/model environment.
