# 12 · Environment

Development baseline:
- Python 3.13+ (target production compatibility: 3.14)
- Node only when Vue/dsh phases require it
- MySQL 8 for intended deployment; SQLite demo mode for self-contained P0 validation
- no Docker requirement

Windows quick commands are in `scripts/*.ps1`.
